---

---

--- Before Day-1 ---
I already knew basic Python programming and had used GitHub repositories before. However, I had limited exposure to Linux command-line environments and filesystem navigation using terminal commands.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

- [x] I can open a Linux terminal and recognize the shell prompt (`user@machine:~$`)
- [x] I can run `python --version`, `git --version`, and `uv --version` without errors
- [x] I can open VS Code connected to WSL/Linux using `code .`
- [x] I know what `~` means and can navigate to my home directory using `cd ~`
- [x] I can create folders and files using `mkdir` and `touch`, and list them with `ls`
- [x] I know the difference between `.xyz`, `./xyz`, and `../xyz`
- [x] I know that in Linux, everything is a file
- [x] I can navigate to the C and D drives within WSL
- [x] I understand the difference between `>` (overwrite) and `>>` (append)
- [x] I have a GitHub account and have created the `tds-bootcamp` repository



--- After Day-1 ---
I learned how WSL acts as a bridge between Windows and Linux, how Linux organizes files and directories differently from Windows, how hidden files work, and how terminal commands can be used to efficiently navigate and manage the filesystem. I also gained a better understanding of relative versus absolute paths and basic shell redirection.
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

--- Feedback (Suggestions for the TDS Team) ---
The live demonstrations were very useful, especially the WSL installation walkthrough and filesystem navigation examples. It would be helpful to provide a downloadable cheat sheet containing all commands demonstrated during the session along with a few practice exercises for beginners to reinforce the concepts after the session.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

---

---

Personal Notes:

* Linux home directory differs from Windows user directory.
* `ls -a` shows hidden files.
* `.` refers to current directory.
* `..` refers to parent directory.
* Use `>` to overwrite and `>>` to append output.
* GitHub repositories can be used to track learning progress through markdown files.
